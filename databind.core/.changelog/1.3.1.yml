changes:
- type: fix
  component: general
  description: Dataclass adapted to ObjectType with no schema fields if a field cannot
    be adapted
  fixes:
  - '#23'
- type: feature
  component: general
  description: support resolving forward references in PEP585 generic aliases
  fixes:
  - '#24'
- type: change
  component: general
  description: change `ConverterProvider.get_converter()` to `ConverterProvider.get_converters()`
    and `ConversionNotApplicable` error type, allowing to test multiple converters
    for a type
release_date: '2022-01-08'
